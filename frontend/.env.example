# Leave unset for local dev — vite.config.js proxies /api to localhost:5000.
# In Vercel's project settings, set this to your deployed Render backend URL.
# The service root or a URL ending in /api both work, e.g.:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=
